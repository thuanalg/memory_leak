#include <stdlib.h>
#include <stdio.h>
#define uint unsigned int
int get_next_rcom(uint* arr, uint n);
int get_next_rcom(uint* arr, uint n) {
	return 0;
}
int main(int argc, char* argv[]) {
	return 0;
}